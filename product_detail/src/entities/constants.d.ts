export type LangC = {
  en: {
    productInformation: LangProductInformation;
  };
};

export type LangProductInformation = {
  color: {
    subTitle: string;
  };
  size: {
    subTitle: string;
  };
  addCart: string;
};
